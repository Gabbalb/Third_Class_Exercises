#include <iostream>

using namespace std;

const int DIM = 100;
int n[DIM];

int main() {
    int giri;
    int index = 0;
    cin >> giri;
    for (int i = 0; i < giri; ++i) {
        int nvalori;
        cin >> nvalori;
        for (int j = 0; j < nvalori; ++j) {
            cin >> n[index];
            index++;
        }
    }

    for (int i = 0; i < giri; ++i) {
        for (int j = 0; j < ; ++j) {

        }
    }

    return 0;
}
