#include <iostream>
using namespace std;

int main() {
    int depth = -1, pre;
    ifstream in("input.txt");
    string command;
    int value;
    while (in >> command >> value){
        ecc.
    return 0;
}
