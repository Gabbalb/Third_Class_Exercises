#include <iostream>

using namespace  std;

int main() {

    int n = 10;
    int somma = 0;
    //ci sono 3 parti separate da ;
    //la prima parte è l'inizializzazione e viene fatta subito
    //la seconda parte è una condizione, se è vera il ciclo entra se falaso esce
    //viene testata appena fatta l'inizializzazione, come il while
    //l'ultima parte in generale aggiorna la variabile di ciclo
    for (int i = 1; i <= n; ++i /* i = i + 1 */) {
        somma += i;


                }

            }

        }
    }
    cout << somma << endl;
    return 0;
}
